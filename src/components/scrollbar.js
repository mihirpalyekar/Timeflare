// import relevant hooks, components and css styles

import SimpleBar from 'simplebar-react';
import { styled } from '@mui/material/styles';

// create scrollbar styled as a simplebar
export const Scrollbar = styled(SimpleBar)``;
